package com.example.myapplication.basic_03_android_test.todoList.di

/*
import android.content.Intent
import com.example.myapplication.basic_03_android_test.todoList.*
import com.example.myapplication.util.dagger.ActivityScope
import dagger.BindsInstance
import dagger.Subcomponent

@Subcomponent(modules = [TodoListViewModule::class])
@ActivityScope
interface TodoListComponent {

    @Subcomponent.Factory
    interface Factory {
        fun create(@BindsInstance activity: TodoListActivity, @BindsInstance startIntent : Intent): TodoListComponent
    }

    fun inject(activity : TodoListActivity)
    fun inject(fragment : TodoListFragment)
    fun inject(fragment: TodoTimeEditFragment)
    fun inject(fragment: TodoTitleEditFragment)
    fun inject(fragment : TodoPhotoEditFragment)
}*/
